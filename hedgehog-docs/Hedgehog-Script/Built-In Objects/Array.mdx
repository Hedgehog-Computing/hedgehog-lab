
import { MathJax, MathJaxContext } from 'better-react-mathjax'

#  Arrays

The `Array` in Hedgehog Script is quite useful for storing and managing data.
 

You can create and print an array:

<iframe src="https://hlab.app/s/docs/array1_hbook"
width="100%"
    height="250px"
    title="what4"/>


You can add or remove items from an array:

<iframe src="https://hlab.app/s/docs/array2_hbook"
width="100%"
    height="600px"/>




:::tip
Use the length method to return the length of an `Array` (`arrayName.length`)! 

And to reference elements simply call `arrayName[indexOfElement]`. 
:::

You can iterate over an array:

<iframe src="https://hlab.app/s/docs/array3_hbook"
width="100%"
    height="500px"
    title="what6"/>


For the use of arrow functions in this example, if you're not familiar with the concept, this is a good reference: [Arrow Functions](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions)


Lastly, here's an example of an `Array` being used for data storage and management (an `Array` object is required as a parameter):


<iframe src='https://hlab.app/s/book/math_introduction_lu' width='100%' height='300px' />
