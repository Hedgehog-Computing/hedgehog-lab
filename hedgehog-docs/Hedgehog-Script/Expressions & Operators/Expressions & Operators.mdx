import { MathJax, MathJaxContext } from 'better-react-mathjax'


# Expressions & Operators

:::warning 

**Never** use equality (`==`), **always** use identity (`===`) to evaluate sameness. There are issues that can cause confusion or unexpected results with equality (`==`):::