{
  "name": "hedgehog-cli",
  "version": "1.0.0",
  "description": "Hedgehog CLI is a command line tool for Hedgehog Script, which allows you to run Hedgehog Script on Node.js from the command line.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "env TS_NODE_COMPILER_OPTIONS='{\"module\": \"commonjs\" }' mocha -r ts-node/register 'test/**/*.ts'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hedgehogcomputing/core": "^0.7.0",
    "@types/node": "^18.7.16",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}
